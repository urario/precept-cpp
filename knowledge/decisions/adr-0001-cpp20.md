---
type: Architecture Decision
title: Use C++20 as the minimum language version
description: Require C++20 so Precept can build on std::span instead of shipping a compatibility span.
status: draft
generated:
  by: claude-code/2.1.231
  at: 2026-08-13T00:00:00+09:00
sources:
  - id: issue-1
    resource: https://github.com/urario/precept-cpp/issues/1
    title: Roadmap issue defining Precept's background, goals, principles, and roadmap
    author: human:urario
  - id: issue-5
    resource: https://github.com/urario/precept-cpp/issues/5
    title: Implementation issue for at_least_span and non_empty_span, listing non-goals
    author: human:urario
tags: [architecture, cpp20, span]
---

# Context

Precept's first release expresses size preconditions over contiguous ranges. That requires
a non-owning contiguous view type. `std::span` provides exactly that and became standard in
C++20.

Supporting C++17 would mean shipping a compatibility span, or depending on an external one.
Both options put a view type — the most fundamental type in the library — outside the
standard, and force conversion boundaries on users who already have `std::span`.

# Decision

C++20 is the minimum supported language version. Precept uses `std::span` directly and does
not reinvent, vendor, or wrap a compatibility span.

# Alternatives considered

* **Support an older language version by supplying a span of Precept's own** — issue #1
  records that the main reason for the C++20 baseline is not reinventing `std::span`, and
  issue #5 records implementing a C++17 compatibility span as a non-goal.

The sources record that rejection, not a fuller comparison behind it. No other alternatives
are recorded, and none are reconstructed here.

# Consequences

* Users on C++17 or earlier cannot adopt Precept. This is accepted.
* CMake targets express the requirement with `cxx_std_20` so the requirement propagates to
  consumers instead of failing later with obscure errors.
* Precept interoperates with any code that already uses `std::span`, with no conversion
  layer.
* Other C++20 facilities may be used where they help, but the C++20 baseline exists for
  `std::span`; adopting anything with a heavier cost still needs its own justification.

# Status

Accepted.

# Related

* Issue [#1](https://github.com/urario/precept-cpp/issues/1) — roadmap and base policy
* Issue [#4](https://github.com/urario/precept-cpp/issues/4) — v0.1 span API contract
* [ADR-0002: Header-only with zero consumer dependencies](adr-0002-header-only.md)
* [Project Charter](../vision/project-charter.md)
